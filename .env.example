# Database connection URL
DATABASE_URL="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority&appName=<app-name>"

# Authentication secret key (used for securing authentication tokens)
AUTH_SECRET="<your_auth_secret>"

# OpenAI API key (used for accessing OpenAI services)
OPENAI_API_TWO="<your_openai_api_key>"

# R2 Cloud Storage credentials
R2_ACCESS_KEY_ID="<your_r2_access_key_id>"
R2_SECRET_ACCESS_KEY="<your_r2_secret_access_key>"
R2_BUCKET_NAME="<your_r2_bucket_name>"
R2_ACCOUNT_ID="<your_r2_account_id>"
R2_PUBLIC_DOMAIN="<your_r2_public_domain_url>"

# Pinecone API key (used for vector database indexing and retrieval)
PINECONE_API_KEY="<your_pinecone_api_key>"

# Public base URL for the application
# NEXT_PUBLIC_BASE_URL="https://your-app-url.com" This should be commented out on local development but for production
# should be filled in with the URL of the deployed application